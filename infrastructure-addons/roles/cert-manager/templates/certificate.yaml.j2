apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-cert
  namespace: "cert-manger" 
spec:
  dnsNames:
  - "*.{{ domain }}"
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  secretName: "cluster-cert"
  commonName: "*.{{ domain }}"